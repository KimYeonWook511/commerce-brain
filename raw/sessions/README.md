# raw/sessions

플랫폼별 작업/대화 덤프를 `sessions/<platform>/`에 올린다 (backend·frontend·infra, 예정: android·ios).
보통 코드 repo의 brain 연동 저장 skill이 자동으로 쓴다. 파일명 `YYYY-MM-DD-<slug>.md`.
이 README는 안내용 placeholder다(ingest/lint 후보 아님).
